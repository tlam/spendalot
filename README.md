Personal expense web app

## Set virtualenv

    python -m venv .venv --prompt spendalot

## Pre-requisites

    sudo apt install libpq-dev
    sudo apt-get install python3-dev
    sudo docker compose up -d

## Vue

From `frontend`

    npm run dev
